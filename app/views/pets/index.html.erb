<p id="notice"><%= notice %></p>

<h1>Pets</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Race</th>
      <th>Birthdate</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pets.each do |pet| %>
      <tr>
        <td><%= pet.name %></td>
        <td><%= pet.race %></td>
        <td><%= pet.birthdate %></td>
        <td><%= link_to 'Show', pet %></td>
        <td><%= link_to 'Edit', edit_pet_path(pet) %></td>
        <td><%= link_to 'Destroy', pet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!-- OJO: Aquí esto es un link_to, que va a generar una etiqueta <a...>...</a>, solo que como yo le agrego la clase en class: "btn btn-primary",
ENTONCES SOLO TIENE ASPECTO DE BOTON. Pero sigue siendo un <a> -->
<%= link_to 'New Pet', new_pet_path, class: "btn btn-primary" %>
